import { component$, useClientEffect$, useStore } from '@builder.io/qwik';

export const Answer = component$(() => {
    return (
        <div>
            <h1>Open Ai 답변</h1>
            <div>답변 작성</div>
        </div>
    )
})